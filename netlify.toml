# Netlify – frontend (React) for "reading from root"
[build]
  base    = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
